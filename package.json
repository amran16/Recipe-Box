{
  "name": "recipe_box",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://amran16.github.io/Recipe-Box",
  "dependencies": {
    "react": "^15.6.1",
    "react-bootstrap": "^0.31.2",
    "react-dom": "^15.6.1",
    "react-modal": "^2.3.2",
    "react-scripts": "1.0.12"
  },
  "scripts": {
    "start": "export PORT=7000 && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0"
  }
}
